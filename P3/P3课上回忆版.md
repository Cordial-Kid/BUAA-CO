# P3课上

## rev

将GPR[rt] 的高index位翻转后放到低位，剩下的位数顺序排列到高位。然后将结果写到GPR[rt]

## blz

大概思路是数出GPR[rs] GPR[rt]的前导零的数量，然后比大小决定NPC的操作，以及向GRF写入tmp1或tmp2

**重点在数前导0**，记的特判。

## lwf

lwf base rt offset
从offset(base)的内存中读出一个数据 
判断这个数和rt的and的各个数位之和的奇偶性 
奇数则存入(rt+1)mod32寄存器 
偶数则存入rt寄存器

**重点在奇偶比较**

